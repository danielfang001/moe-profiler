This branch is for getting and analyzing router logits.

To run the OLMOE model on some samples: `scripts/sample_mmlu_logits.py --num_samples 10`

To analyze the saved logits, see `logit_analysis/analysis.ipynb`